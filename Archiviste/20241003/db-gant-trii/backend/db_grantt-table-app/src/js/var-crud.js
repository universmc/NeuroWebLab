// script-crud.js

function createTask(taskData) {
    // Logique pour créer une nouvelle tâche
}

function readTask(taskId) {
    // Logique pour lire les détails d'une tâche
}

function updateTask(taskId, updatedData) {
    // Logique pour mettre à jour une tâche
}

function deleteTask(taskId) {
    // Logique pour supprimer une tâche
}

// Exportez ces fonctions si vous utilisez des modules
// export { createTask, readTask, updateTask, deleteTask };
